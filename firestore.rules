rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Since only the Express backend connects to Firestore, we allow read and write
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
